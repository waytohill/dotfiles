# Ranger configuration for Ghostty image previews
# Ghostty supports the Kitty graphics protocol, which ranger can use
# to render image previews automatically.

set preview_images true
set preview_images_method kitty
set preview_files true
